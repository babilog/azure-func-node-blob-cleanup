{
  "name": "azure-func-node-blob-cleanup",
  "version": "1.0.0",
  "description": "Runs as an Azure function, cleans up blob storage per scheduled trigger",
  "main": "./azurefunc-file-storage-cleanup/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gabriel Babilonia",
  "license": "ISC",
  "dependencies": {
    "azure-storage": "^2.5.0",
    "ramda": "^0.25.0"
  }
}
